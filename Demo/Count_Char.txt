ldr r0,=string
mov r1, #0 
loop:
   ldrb r9, [r0]
   cmp  r9, #0
   beq exit

   add r1, #1
   add r0, #1
   b loop
exit:
   mov r10, r1

.data
string: .asciz "Computer Architecture Module preparation questions"
return: .word 0