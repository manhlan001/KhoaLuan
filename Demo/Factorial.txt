mov r0, #1
mov r1, #1
ldr r2, =num
ldr r2, [r2]
ldr r3, =result
do:
    mul r0, r0, r1
    add r1, r1, #1
    cmp r2, r1
    blt exit
    b do
exit:
    str r0, [r3]
    mov r10, #10

.data
num: .word 5
result: .word 0