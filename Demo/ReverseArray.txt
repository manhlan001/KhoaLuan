mov r10, #0
ldr r0, =array1
ldr r1, =array2
mov r6, r0

counter:
    ldr r2, [r0]
    cmp r2, #0
    beq stop_counting
    add r10, r10, #1
    add r0, r0, #4
    b counter

stop_counting:
    sub r0, r0, #4
    mov r9, r10
    mov r8, #0

swap:
    ldr r3, [r0]
    str r3, [r1]
    sub r9, r9, #1
    add r1, r1, #4
    sub r0, r0, #4
    cmp r9, #0
    beq exit
    b swap

exit:
    mov r12, #10

.data
array1: .word 1, 2, 3, 4, 5, 6
space_array: .zero 4
array2: .space 24