ldr r0, =n
ldr r1, [r0]
mov r2, #0
mov r3, #1
mov r4, #0
ldr r5, =result

cmp r1, #0
beq store_zero

cmp r1, #1
beq store_one

loop:
    cmp r4, r1
    beq end

    str r2, [r5, r4, lsl #2]

    add r6, r2, r3
    mov r2, r3
    mov r3, r6

    add r4, r4, #1
    b loop

store_zero:
    str r2, [r5]
    b end

store_one:
    str r3, [r5]
    b end

end:
    mov r7, #1

.data
n: .word 10
return: .zero 4
result: .space 40